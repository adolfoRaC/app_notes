import 'package:flutter/material.dart';

class AppColors {
  // Colores primarios (morado)
  static const Color primaryLight = Color(0xFF9C27B0); // Lila claro
  static const Color primaryDark = Color(0xFF7B1FA2); // Morado más profundo

  // Colores secundarios (tonos alternativos de morado)
  static const Color secondaryLight = Color(0xFF000000);
  static const Color secondaryDark = Color(0xFFFFFFFF);

  // Colores de fondo/superficie
  static const Color surfaceLight = Color(0xFFFFFEFE);
  static const Color surfaceDark = Color(0xFF030303);

  // Colores de texto o elementos encima del fondo
  static const Color onSurfaceLight = Color(0xFF131313);
  static const Color onSurfaceDark = Color(0xFFEAEAEA);
}
